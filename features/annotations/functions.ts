const add = (a: number, b: number) => {
  return a + b;
};

// Return void, but we want return a number acutally
// const subtract = (a: number, b: number) => {
//   a - b;
// }

const subtract = (a: number, b: number): number => {
  return a - b;
};
